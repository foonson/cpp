#ifndef _UTIL_USTRING
#define _UTIL_USTRING

#include "pch.h"

namespace UString {
  string toString(int i) {
    char buffer[50];
    sprintf(buffer, "%d", i);
    string r = buffer;
    return r; 
  }
}

#endif
